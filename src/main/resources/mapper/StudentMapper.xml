<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chouyv.mapper.StudentMapper">

    <resultMap id="BaseResultMap" type="cn.chouyv.domain.Student">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="username" column="username" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="role" column="role" jdbcType="TINYINT"/>
            <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
            <result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP"/>
            <result property="isDeleted" column="is_deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,
        role,created_at,
        updated_at,is_deleted
    </sql>
    <insert id="addStudentAddress">
        INSERT INTO shopping_info(uid,name,location,phone) VALUES (#{uid},#{name},#{location},#{phone})
    </insert>
    <!--TODO * 替换成对应的字段!!! -->
    <select id="selectOneByUsername" resultType="cn.chouyv.domain.Student">
        SELECT * FROM student WHERE username = #{username} AND is_deleted = 0
    </select>
    <select id="selectOneById" resultType="cn.chouyv.domain.Student">
        SELECT * FROM student WHERE id = #{id} AND is_deleted = 0
    </select>
</mapper>
